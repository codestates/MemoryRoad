/* [ interface 예시 ]
 * 
 * interface UserInfo {
 *   id: number | null;
 *   name: string | null,
 *   email: string | null ...
 * }
 * 
 * export type State = {
 *   userInfo: UserInfo;
 * }
 * 
 * export const initialState:State = {
 *   userInfo : {id: null, name: null, email: null ...}
 * };
 * 
 */

export {}